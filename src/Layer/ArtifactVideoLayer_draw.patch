    if (impl_->currentQImage_.isNull()) {
        decodeCurrentFrame();
    }

    if (impl_->currentQImage_.isNull()) return;

    auto size = sourceSize();
    renderer->drawSprite(0.0f, 0.0f, (float)size.width, (float)size.height, impl_->currentQImage_, opacity());
}